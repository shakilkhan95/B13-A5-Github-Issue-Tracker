This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
assets/Aperture.png
assets/bug.png
assets/closed-Status.png
assets/documentation.png
assets/enhancement.png
assets/github-logo.png
assets/good-first-issue.png
assets/help-wanted.png
assets/open-Status.png
GitHub Issues Tracker.fig
index.html
main.html
README.md
scripts/login.js
scripts/main.js
styles/style.css
tailwind.config.js
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="tailwind.config.js">

</file>

<file path="README.md">
- Create a readme file and answer this question on your own. Don’t copy-paste from Google or any AI chatbot. 
    - 1️⃣ What is the difference between var, let, and const?
    - Answer: Var is global scope variable and let and const are block scope variable
    - 
    - 2️⃣ What is the spread operator (...)?
    - Answer: spread operator is used to spread or expand elements of array, string, and object.
    - 
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    - Answer: map() loop through all elements and applied the condition then return a new array. filter() loop through elements based on condition provided and return a new array. forEach() loop through all elements and applied condition to all of them and don't return any values.
    - 
    - 4️⃣ What is an arrow function?
    - Answer: It's an ES6 feature. It's shorter and cleaner version of regular function.
    - 
    - 5️⃣ What are template literals?
    - It's used to write string with backtick (``) instead of quote (""). It's give us the power to use variables, embed expressions and lot of things in string. It's also allows us to write multiline string.
</file>

<file path="scripts/login.js">
document.getElementById('sign-in-btn').addEventListener('click', () => {
    const defaultUserName = 'admin';
    const defaultPass = 'admin123';
    const userNameInput = document.getElementById('user-name');
    const userName = userNameInput.value;
    const passInput = document.getElementById('password');
    const userPass = passInput.value;
    
    //condition if the username isn't valid
    if(userName !== defaultUserName){
        alert('Wrong user name. Please enter valid user name');
        return;
    }

    // condition if password isn't valid 
    if(userPass !== defaultPass){
        alert("Wrong password. Please enter valid password");
        return;
    }

    alert("Login Successful");
    return window.location.href = "../main.html";
});
</file>

<file path="styles/style.css">
* {
    font-family: "Geist", sans-serif;
}

/*status */
.open{
    background-color: #00A96E;
    color: ;
}

/* priority */
.high {
    background-color: #FEECEC;
    color: #EF4444;
}

.medium {
    background-color: #FFF6D1;
    color: #F59E0B;
}

.low {
    background-color: #EEEFF2;
    color: #9CA3AF;
}

/* labels */
.bug {
    background-color: #FEECEC;
    color: #EF4444;
    border-color: #EF4444;
}

.help-wanted {
    background-color: #FFF8DB;
    color: #D97706;
    border-color: #D97706;
}

.enhancement {
    background-color: #DEFCE8;
    color: #00A96E;
    border-color: #00A96E;
}

.good-first-issue {
    background-color: #F3E8FF;
    color: #9333EA;
    border-color: #9333EA;
}

.documentation {
    background-color: #E8F1FF;
    color: #2563EB;
    border-color: #2563EB;
}
</file>

<file path="index.html">
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Github Issue Tracker | Login</title>

    <!-- tailwindcss & daisyUI cdn link  -->
    <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <!-- Google font link  -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Geist:wght@100..900&display=swap" rel="stylesheet">

    <!-- fontAwesome cdn link  -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css"
        integrity="sha512-2SwdPD6INVrV/lHTZbO2nodKhrnDdJK9/kg2XD1r9uGqPo1cUbujc+IYdlYdEErWNu69gVcYgdxlmVmzTWnetw=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- custom css link  -->
    <link rel="stylesheet" href="./styles/style.css">

    <!-- favIcon link  -->
     <link rel="shortcut icon" href="./assets/github-logo.png" type="image/x-icon">
</head>
<body class="bg-[#F8FAFC] py-10 px-5">
    <section class="bg-base-100 py-10 px-5 max-w-sm flex flex-col justify-center items-center mx-auto shadow-md">
        <header class="text-center">
            <div class="flex justify-center mb-5"><img src="./assets/github-logo.png" alt="Github Logo"></div>
            <h2 class="text-2xl font-semibold text-[#1F2937]">GitHub Issues Tracker</h2>
            <p class="text-[#64748B]">Sign in to manage your issues</p>
        </header>

        <div class="w-full mt-5">
            <fieldset class="fieldset">
                <label class="text-[#1F2937] font-medium text-base">Username</label>
                <input id="user-name" type="text" class="input w-full" placeholder="Enter Username" />
                <p class="text-sm text-[#4A00FF]">Default: admin</p>
                <label class="text-[#1F2937] font-medium text-base">Password</label>
                <input id="password" type="password" class="input w-full" placeholder="Enter Password" />
                <p class="text-sm text-[#4A00FF]">Default: admin123</p>
                <button type="submit" id="sign-in-btn" class="btn btn-primary mt-4">Sign In</button>

                <div class="flex items-center gap-4 my-5">
                    <div class="flex-1 h-px bg-gray-500"></div>
                    <span class="text-base text-[#1F2937]">OR</span> 
                    <div class="flex-1 h-px bg-gray-500"></div>
                </div>

                <p class="text-sm text-[#64748B] text-center">Demo Credentials</p>

                <div class="rounded-lg bg-[#F8FAFC] text-center py-3">
                    <p class="text-base font-semibold text-[#1F2937]">Username: <span class="font-normal">admin</span></p>
                    <p class="text-base font-semibold text-[#1F2937]">Password: <span class="font-normal">admin123</span></p>
                </div>
            </fieldset>
        </div>
    </section>

    <script src="./scripts/login.js"></script>
</body>
</html>
</file>

<file path="main.html">
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Github Issue Tracker | Main</title>

    <!-- tailwindcss & daisyUI cdn link  -->
    <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <!-- Google font link  -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Geist:wght@100..900&display=swap" rel="stylesheet">

    <!-- fontAwesome cdn link  -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css"
        integrity="sha512-2SwdPD6INVrV/lHTZbO2nodKhrnDdJK9/kg2XD1r9uGqPo1cUbujc+IYdlYdEErWNu69gVcYgdxlmVmzTWnetw=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- custom css link  -->
    <link rel="stylesheet" href="./styles/style.css">

    <!-- favIcon link  -->
    <link rel="shortcut icon" href="./assets/github-logo.png" type="image/x-icon">
</head>

<body class="bg-[#F8FAFC]">
    <!-- header section starts here  -->
    <header class="py-6 px-20 bg-base-100 flex flex-col md:flex-row gap-5 md:justify-between items-center rounded-lg ">
        <div class="flex items-center gap-2">
            <div><img class="w-12" src="./assets/github-logo.png" alt="Github logo"></div>
            <h2 class="text-2xl font-semibold text-[#1F2937]">GitHub Issues Tracker</h2>
        </div>

        <div class="flex">
            <div>
                <input id="search-input" class="input rounded-lg" type="text" placeholder="Search issues" required />
            </div>
            <button id="search-btn" class="btn btn-primary rounded-lg ml-2">Search</button>
        </div>
    </header>

    <!-- main section starts here  -->
    <section class="my-10 w-11/12 md:w-10/12 mx-auto space-y-5">
        <!-- tabs section -->
        <div id="status-tabs" class="flex bg-base-100 px-4 py-5 gap-5 justify-center md:justify-start">
            <button value="all" class="status-btn btn btn-primary md:w-28" type="button">All</button>
            <button value="open" class="status-btn btn md:w-28" type="button">Open</button>
            <button value="closed" class="status-btn md:w-28 btn" type="button">Closed</button>
        </div>

        <!-- section header to show status -->
        <div class="flex bg-base-100 p-4 gap-5 justify-between items-center">
            <div class="flex items-center gap-2">
                <div><img src="./assets/Aperture.png"></div>

                <div>
                    <h3 class="text-lg md:text-xl font-semibold text-[#1F2937]"><span id="issues-counter"></span> Issues</h3>
                    <p class="text-[#64748B] text-xs md:text-base w-10/12 md:w-full">Track and manage your project issues</p>
                </div>
            </div>

            <div class="flex flex-col md:flex-row gap-5">
                <div class="flex gap-1 items-center">
                    <div><img src="./assets/Open-Status.png"></div>
                    <p class="font-medium text-xs md:text-base text-[#1F2937]">Open</p>
                </div>

                <div class="flex gap-1 items-center">
                    <div><img src="./assets/Closed-Status.png"></div>
                    <p class="font-medium text-xs md:text-base text-[#1F2937]">Closed</span></p>
                </div>
            </div>
        </div>

        <!-- spinner  -->
        <div id="spinner" class="text-center py-20 hidden"><span class="loading loading-bars loading-xl"></span></div>

        <!-- cards container starts here -->
        <section id="issues-container" class="mx-3 bg-[#FBFBFB] grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">
            <!-- cards will appear here  -->
        
        </section>

        <!-- modal section  -->
         <section>
            <!-- Open the modal using ID.showModal() method -->
            <dialog id="issue_details" class="modal">
                <div class="modal-box">
                    
                        <div id="details-container"></div>

                        <form method="dialog">
                            <!-- if there is a button in form, it will close the modal -->
                            <button class="btn btn-primary">Close</button>
                        </form>
                    </div>
                </div>
            </dialog>
         </section>
         
    </section>

    <script src="./scripts/main.js"></script>
</body>

</html>
</file>

<file path="scripts/main.js">
let activeStatus = "all";
const statusTabs = document.querySelectorAll(".status-btn");
statusTabs.forEach((tab) => {
  tab.addEventListener("click", () => {
    activeStatus = tab.value;

    toggleStatus(tab);
    loadCommits(activeStatus);
    setCounter(activeStatus);
  });
});

//function to toggle status styles based on clicking button
const toggleStatus = (clickedTab) => {
  statusTabs.forEach((tab) => {
    tab.classList.remove("btn-primary");
  });
  clickedTab.classList.add("btn-primary");
};

// function to fetch data from api
const loadCommits = async (status) => {
    controlSpinner(true);
  const issuesUrl = "https://phi-lab-server.vercel.app/api/v1/lab/issues";
  const res = await fetch(issuesUrl);
  const issues = await res.json();

  displayIssues(issues.data, status);
};

const displayIssues = (issues, status) => {
  // Get the issue card container and make it empty
  const issuesContainer = document.getElementById("issues-container");
  issuesContainer.innerHTML = "";

  issues.forEach((issue) => {
    //get the priority texts
    const priority = issue.priority;
    if (status === "all") {
      const issueDiv = document.createElement("div");
      issueDiv.innerHTML = `
                <!-- card  -->
            <div onclick="loadDetails(${issue.id})" class="h-full flex flex-col justify-between rounded-md bg-base-100 shadow border-t-4 border-t-[${borderColor(issue.status)}] transition-all hover:scale-101">
                <div class="p-4 space-y-3">
                    <div class="flex justify-between">
                        <div><img src="./assets/${issue.status}-Status.png"></div>
                        <p class="w-fit px-8 py-2 rounded-full ${priority} text-xs font-medium">${issue.priority.toUpperCase()}</p>
                    </div>
                    
                    <div class="space-y-2">
                        <h3 class="text-sm font-semibold text-[#1F2937]">${issue.title}</h3>
                        <p class="text-xs text-[#64748B]">${issue.description}</p>
                    </div>
                    
                    <!-- labels container -->
                    <div class="flex gap-2"> ${displayLabel(issue.labels)} </div>
                </div>

                <div class="flex justify-between  border-t-2 border-t-[#EFEFEF]">
                    <div class="p-4 space-y-3">
                        <p class="text-xs text-[#64748B]">Author: ${issue.author}</p>
                        <p class="text-xs text-[#64748B]">Assignee: ${issue.assignee}</p>
                        
                    </div>

                    <div class="p-4 space-y-3">
                        <p class="text-xs text-[#64748B]">${issue.createdAt}</p>
                        <p class="text-xs text-[#64748B]">${issue.updatedAt}</p>
                    </div>
                </div>
            </div>
            `;

      issuesContainer.append(issueDiv);
      controlSpinner(false);
      return;
    }
    if (issue.status === status) {
      const issueDiv = document.createElement("div");
      issueDiv.innerHTML = `
                <!-- card  -->
            <div onclick="loadDetails(${issue.id})" class="h-full flex flex-col justify-between rounded-md bg-base-100 shadow border-t-4 border-t-[${borderColor(issue.status)}] transition-all hover:scale-101  other-card">
                <div class="p-4 space-y-3">
                    <div class="flex justify-between">
                        <div><img src="./assets/${issue.status}-Status.png"></div>
                        <p class="w-fit px-8 py-2 rounded-full ${priority} text-xs font-medium">${issue.priority.toUpperCase()}</p>
                    </div>
                    
                    <div class="space-y-2">
                        <h3 class="text-sm font-semibold text-[#1F2937]">${issue.title}</h3>
                        <p class="text-xs text-[#64748B]">${issue.description}</p>
                    </div>
                    
                    <!-- labels container -->
                    <div class="flex gap-2"> ${displayLabel(issue.labels)} </div>
                </div>

                <div class="flex justify-between  border-t-2 border-t-[#EFEFEF]">
                    <div class="p-4 space-y-3">
                        <p class="text-xs text-[#64748B]">Author: ${issue.author}</p>
                        <p class="text-xs text-[#64748B]">Assignee: ${issue.assignee}</p>
                        
                    </div>

                    <div class="p-4 space-y-3">
                        <p class="text-xs text-[#64748B]">${issue.createdAt}</p>
                        <p class="text-xs text-[#64748B]">${issue.updatedAt}</p>
                    </div>
                </div>
            </div>
            `;

      issuesContainer.append(issueDiv);
      controlSpinner(false);
      return;
    }
  });
};

//function to set border color based on status
const borderColor = (status) => {
  if (status === "open") {
    return "#00A96E";
  }
  if (status === "closed") {
    return "#A855F7";
  }
};

//function to display labels
const displayLabel = (labels) => {
  const createElement = labels.map(
    (el) =>
      `<div class="w-fit px-2 py-1 rounded-full flex items-center gap-1 text-xs border ${el.split(" ").join("-")}"><div><img src="../assets/${el.split(" ").join("-")}.png"></div><div>${el.toUpperCase()}</div></div>`,
  );
  return createElement.join(" ");
};

//function to set counter
const setCounter = (status) =>{
    const counter = document.getElementById("issues-counter");

    fetch("https://phi-lab-server.vercel.app/api/v1/lab/issues")
    .then(res=> res.json())
    .then(data => {
        const issues = data.data;
        if(status === 'all'){
            counter.innerText = issues.length;
            return;
        }
        else{
            const filteredIssues = issues.filter(issue => issue.status === status);
            counter.innerText = filteredIssues.length;
            return;
        }

    })
};

//function to load issue details by clicking on issue card
const loadDetails = (id) => {
    fetch(`https://phi-lab-server.vercel.app/api/v1/lab/issue/${id}`)
    .then(res=> res.json())
    .then(data => displayDetails(data.data));
};

//function to display details with a modal
const displayDetails = (issues) => {
    const detailsContainer = document.getElementById("details-container");
    detailsContainer.innerHTML = `
        <!-- details card  -->
            <div class="bg-white w-11/12 max-w-[700px] mx-auto rounded-lg p-6 space-y-6">
                <h1 class="text-xl font-bold text-[#1F2937]">${issues.title}</h1>

                <div class="flex gap-3 flex-col md:flex-row justify-center items-center md:justify-start">
                    <span class="px-3 py-2 rounded-full bg-[${borderColor(issues.status)}] text-white text-xs font-medium w-fit">${issues.status.toUpperCase()}</span>
                    <span class="text-[#64748B] text-xs">Opened by ${issues.author}</span>
                    <span class="text-[#64748B] text-xs">${issues.updatedAt}</span>
                </div>

                <div class="flex items-center gap-4">
                    <div class="flex flex-col md:flex-row gap-2"> ${displayLabel(issues.labels)} </div>
                </div>

                <p class="text-base text-[#64748B]">${issues.description}</p>

                <div class="bg-[#F8FAFC] rounded-lg p-6 flex justify-between">
                    <div class="space-y-2">
                        <p class="text-base text-[#64748B]">Assignee:</p>
                        <h3 class="text-base font-semibold text-[#1F2937]">${issues.assignee}</h3>
                    </div>

                    <div class="space-y-2">
                        <p class="text-base text-[#64748B]">Priority</p>
                        <p class="w-fit px-8 py-2 rounded-full text-xs font-medium ${issues.priority}">${issues.priority.toUpperCase()}</p>
                    </div>
                </div>
            </div>
    `;
    document.getElementById("issue_details").showModal();
}

// function to control spinner 
const controlSpinner = (status) => {
    if(status){
        document.getElementById("spinner").classList.remove('hidden');
        document.getElementById("issues-container").classList.add("hidden");
    } else{
        document.getElementById("spinner").classList.add("hidden");
        document.getElementById("issues-container").classList.remove("hidden");
    }
}

// function to search issues
document.getElementById("search-btn").addEventListener('click', () => {
    //get the value from search input 
    const searchInput = document.getElementById("search-input");
    const searchValue = searchInput.value.trim().toLowerCase();
    if(searchValue === ''){
        alert('Enter a word to search');
        return;
    }
    
    // fetch and get the data from api 
    fetch(
      `https://phi-lab-server.vercel.app/api/v1/lab/issues/search?q=${searchValue}`,
    )
      .then((res) => res.json())
      .then((data) => displayIssues(data.data, activeStatus));
});

setCounter(activeStatus);
loadCommits(activeStatus);
</file>

</files>
